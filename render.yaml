services:
  - type: web
    name: process-upscaling-workbench
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r upscaling_pipeline_tool/requirements.txt
    startCommand: python3 -m upscaling_pipeline_tool.app --host 0.0.0.0
    healthCheckPath: /
