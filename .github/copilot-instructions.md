# Upscaling Pipeline Tool

- Work in the current workspace root.
- The active application is `upscaling_pipeline_tool/app.py`.
- Keep the project focused on protocol-to-scale-up workflow mapping.
- Preserve the source-text to blocks to phenomena to groups to alternatives flow.
- Keep arrows flowchart-style, orthogonal, and routed around text/cards.
- Keep hover explanations for phenomena and unit-operation alternatives.
- The app should remain runnable with the Python standard library unless a dependency is explicitly justified.
- Keep `README.md` and `upscaling_pipeline_tool/README.md` aligned with the actual project structure.
